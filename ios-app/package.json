{
  "name": "dynasty-timeline-ios-app",
  "version": "1.0.0",
  "private": true,
  "description": "iOS wrapper for the China History Atlas v3 web app.",
  "scripts": {
    "sync:web": "rm -rf www && mkdir -p www && rsync -a --exclude='*.py' --exclude='test-icon.html' ../v3/ www/",
    "cap:sync": "npm run sync:web && cap sync ios",
    "cap:open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0"
  }
}
